.row
  .col-sm-12.text-center
    %h2 メールアドレスを編集
    %br
.row
  .col-sm-3
  .col-sm-6
    .card.bg-light.mb-3.img-thumbnail.card.border-warning.mb-3
      .card-body.px-2.py-3
        - @account = current_account if @account == nil
        = form_for(@account, as: :accounts, url: ex_email_update_path) do |f|
          - if @account.errors.full_messages.present?
            .alert.alert-danger 入力に不備があります

          - if @error_message.present?
            .alert.alert-danger 
              = @error_message

          .field
            = f.label "メールアドレス"
            %br
            = f.email_field :email, autofocus: true, autocomplete: "email", :class => 'form-control'

          %br
          .row
            .col-sm-6.actions          
              = link_to '戻る', my_page_path, :class => 'pull-right btn btn-secondary'
            .col-sm-6.actions          
              = f.submit "更新", :class => 'btn btn-primary center-block'
                          